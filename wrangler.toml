# Cloudflare Pages Configuration
# This file is optional but provides additional configuration options

name = "idpbuilder"
compatibility_date = "2024-12-19"

# Build configuration
[build]
command = "./build-site.sh"
cwd = ""
watch_dirs = ["site/**/*"]

# Pages builds output to ./public by default
# No [site] or [build.upload] sections needed for Pages deployment

# Environment variables (if needed)
# [env.production]
# [env.production.vars]
# API_URL = "https://api.example.com"
