---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: platforms.idpbuilder.cnoe.io
spec:
  group: idpbuilder.cnoe.io
  names:
    kind: Platform
    listKind: PlatformList
    plural: platforms
    singular: platform
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Platform is the Schema for the platforms API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PlatformSpec defines the desired state of Platform
            properties:
              components:
                description: Components defines the platform component configuration
                properties:
                  gateways:
                    description: Gateways is a list of Gateway provider references
                    items:
                      description: ProviderReference references a provider CR by name,
                        kind, and namespace
                      properties:
                        kind:
                          description: Kind is the kind of the provider CR (e.g.,
                            GiteaProvider, NginxGateway)
                          type: string
                        name:
                          description: Name is the name of the provider CR
                          type: string
                        namespace:
                          description: Namespace is the namespace of the provider
                            CR
                          type: string
                      required:
                      - kind
                      - name
                      - namespace
                      type: object
                    type: array
                  gitOpsProviders:
                    description: GitOpsProviders is a list of GitOps provider references
                    items:
                      description: ProviderReference references a provider CR by name,
                        kind, and namespace
                      properties:
                        kind:
                          description: Kind is the kind of the provider CR (e.g.,
                            GiteaProvider, NginxGateway)
                          type: string
                        name:
                          description: Name is the name of the provider CR
                          type: string
                        namespace:
                          description: Namespace is the namespace of the provider
                            CR
                          type: string
                      required:
                      - kind
                      - name
                      - namespace
                      type: object
                    type: array
                  gitProviders:
                    description: GitProviders is a list of Git provider references
                    items:
                      description: ProviderReference references a provider CR by name,
                        kind, and namespace
                      properties:
                        kind:
                          description: Kind is the kind of the provider CR (e.g.,
                            GiteaProvider, NginxGateway)
                          type: string
                        name:
                          description: Name is the name of the provider CR
                          type: string
                        namespace:
                          description: Namespace is the namespace of the provider
                            CR
                          type: string
                      required:
                      - kind
                      - name
                      - namespace
                      type: object
                    type: array
                type: object
              domain:
                description: Domain is the base domain for the platform
                type: string
            required:
            - components
            - domain
            type: object
          status:
            description: PlatformStatus defines the observed state of Platform
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the Platform's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed Platform
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the Platform (e.g.,
                  Pending, Ready, Failed)
                type: string
              providers:
                description: Providers contains the aggregated status of all provider
                  references
                properties:
                  gateways:
                    description: Gateways contains status of Gateway providers
                    items:
                      description: ProviderStatusSummary summarizes the status of
                        a provider
                      properties:
                        kind:
                          description: Kind is the kind of the provider
                          type: string
                        name:
                          description: Name is the name of the provider
                          type: string
                        ready:
                          description: Ready indicates whether the provider is ready
                          type: boolean
                      required:
                      - kind
                      - name
                      - ready
                      type: object
                    type: array
                  gitOpsProviders:
                    description: GitOpsProviders contains status of GitOps providers
                    items:
                      description: ProviderStatusSummary summarizes the status of
                        a provider
                      properties:
                        kind:
                          description: Kind is the kind of the provider
                          type: string
                        name:
                          description: Name is the name of the provider
                          type: string
                        ready:
                          description: Ready indicates whether the provider is ready
                          type: boolean
                      required:
                      - kind
                      - name
                      - ready
                      type: object
                    type: array
                  gitProviders:
                    description: GitProviders contains status of Git providers
                    items:
                      description: ProviderStatusSummary summarizes the status of
                        a provider
                      properties:
                        kind:
                          description: Kind is the kind of the provider
                          type: string
                        name:
                          description: Name is the name of the provider
                          type: string
                        ready:
                          description: Ready indicates whether the provider is ready
                          type: boolean
                      required:
                      - kind
                      - name
                      - ready
                      type: object
                    type: array
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
