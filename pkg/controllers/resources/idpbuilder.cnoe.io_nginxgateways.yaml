---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: nginxgateways.idpbuilder.cnoe.io
spec:
  group: idpbuilder.cnoe.io
  names:
    kind: NginxGateway
    listKind: NginxGatewayList
    plural: nginxgateways
    singular: nginxgateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.ingressClassName
      name: IngressClass
      type: string
    - jsonPath: .status.loadBalancerEndpoint
      name: LoadBalancer
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: NginxGateway is the Schema for the nginxgateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NginxGatewaySpec defines the desired state of NginxGateway
            properties:
              ingressClass:
                description: IngressClass defines the ingress class configuration
                properties:
                  isDefault:
                    default: true
                    description: IsDefault indicates if this should be the default
                      ingress class
                    type: boolean
                  name:
                    default: nginx
                    description: Name is the name of the ingress class
                    type: string
                type: object
              namespace:
                default: ingress-nginx
                description: Namespace is the namespace where Nginx Ingress Controller
                  will be deployed
                type: string
              version:
                default: 1.13.0
                description: Version is the version of Nginx Ingress Controller to
                  install
                type: string
            required:
            - namespace
            type: object
          status:
            description: NginxGatewayStatus defines the observed state of NginxGateway
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the NginxGateway's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              controller:
                description: Controller contains information about the Nginx controller
                  deployment
                properties:
                  readyReplicas:
                    description: ReadyReplicas is the number of ready replicas
                    format: int32
                    type: integer
                  replicas:
                    description: Replicas is the desired number of replicas
                    format: int32
                    type: integer
                type: object
              ingressClassName:
                description: |-
                  IngressClassName is the name of the ingress class to use in Ingress resources
                  This is a duck-typed field that all Gateway providers must expose
                type: string
              installed:
                description: Installed indicates whether Nginx has been installed
                type: boolean
              internalEndpoint:
                description: |-
                  InternalEndpoint is the cluster-internal API endpoint
                  This is a duck-typed field that all Gateway providers must expose
                type: string
              loadBalancerEndpoint:
                description: |-
                  LoadBalancerEndpoint is the external endpoint for accessing services
                  This is a duck-typed field that all Gateway providers must expose
                type: string
              phase:
                description: Phase represents the current phase of the Nginx gateway
                  (e.g., Pending, Installing, Ready, Failed)
                type: string
              version:
                description: Version is the currently installed version of Nginx
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
