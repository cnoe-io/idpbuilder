---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: argocdproviders.idpbuilder.cnoe.io
spec:
  group: idpbuilder.cnoe.io
  names:
    kind: ArgoCDProvider
    listKind: ArgoCDProviderList
    plural: argocdproviders
    singular: argocdprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: ArgoCDProvider is the Schema for the argocdproviders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ArgoCDProviderSpec defines the desired state of ArgoCDProvider
            properties:
              adminCredentials:
                description: AdminCredentials defines the ArgoCD admin credentials
                  configuration
                properties:
                  autoGenerate:
                    default: true
                    description: AutoGenerate indicates whether to auto-generate credentials
                      if not provided
                    type: boolean
                  secretRef:
                    description: SecretRef references a secret containing the admin
                      credentials
                    properties:
                      key:
                        description: Key is the key within the secret
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                      namespace:
                        description: Namespace is the namespace of the secret
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                type: object
              namespace:
                default: argocd
                description: Namespace is the namespace where ArgoCD will be deployed
                type: string
              projects:
                description: Projects is a list of ArgoCD projects to create
                items:
                  description: ArgoCDProject defines an ArgoCD project to create
                  properties:
                    description:
                      description: Description is the project description
                      type: string
                    name:
                      description: Name is the project name
                      type: string
                  required:
                  - name
                  type: object
                type: array
              version:
                default: v2.12.0
                description: Version is the version of ArgoCD to install
                type: string
            required:
            - namespace
            type: object
          status:
            description: ArgoCDProviderStatus defines the observed state of ArgoCDProvider
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the ArgoCDProvider's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              credentialsSecretRef:
                description: |-
                  CredentialsSecretRef references the secret containing ArgoCD admin credentials
                  This is a duck-typed field that all GitOps providers must expose
                properties:
                  key:
                    description: Key is the key within the secret
                    type: string
                  name:
                    description: Name is the name of the secret
                    type: string
                  namespace:
                    description: Namespace is the namespace of the secret
                    type: string
                required:
                - key
                - name
                - namespace
                type: object
              endpoint:
                description: |-
                  Endpoint is the external URL for ArgoCD web UI
                  This is a duck-typed field that all GitOps providers must expose
                type: string
              installed:
                description: Installed indicates whether ArgoCD has been installed
                type: boolean
              internalEndpoint:
                description: |-
                  InternalEndpoint is the cluster-internal URL for ArgoCD API access
                  This is a duck-typed field that all GitOps providers must expose
                type: string
              phase:
                description: Phase represents the current phase of the ArgoCD provider
                  (e.g., Pending, Installing, Ready, Failed)
                type: string
              version:
                description: Version is the currently installed version of ArgoCD
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
